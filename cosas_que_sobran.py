# This Python file uses the following encoding: utf-8

# if __name__ == "__main__":
#     pass

"""##segunda parte de json

else:

    dict1 = {}
    with open(selectedFile, encoding="utf-8") as archivoTexto:
        for line in archivoTexto:
            command, description = line.strip().split(None, 1)
            dict1[command] = description.strip()

        out_file = open("test1.json", "w")
        json.dump(dict1, out_file, indent = 4, sort_keys = False)
    archivito = json.load(out_file)
    j = len(archivito["MenuSections"])

    for i in range (0,j):
        print(archivito["MenuSections"][i]["Name"])

    out_file.close()
    for archivoJson in datos["TaxRateId"]:
        archivoJson["null"] == archivoJson[12814816]

jsonPrint = json.dumps(datos, indent=4)
print(jsonPrint)

"""

